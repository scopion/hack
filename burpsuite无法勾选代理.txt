自己已经解决问题了，现在分享下过程。通过alerts的提示信息可以知道127.0.0.1:8080不让用，换了好几个IP地址都不可用，最后试了下换了端口号，改为8079.成功了！！！
看来是8080端口号被禁用了。
好吧，让我来查下哪个程序在用8080端口
运行cmd 输入netstat -ano 找到了PID为2660的程序在用
打开任务管理器，在进程中选到PID列，如果没有PID列则在任务管理器“查看”中一项选择“选择列”勾选PID。
在下面的PID中我找到了ApplicationWebServer.exe这个进程，右键打开本地位置。
发现原来是电路分析实验课上用的NI Circuit软件占用了，好吧。把这破玩意删除了就一切太平